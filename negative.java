import java.io.*;
import java.util.scanner;
import java.util.arrays;
public class negative
{
   public static void main(String[] args)
   {
   int[] num={3,2,-1,-2,-3};
   Sytem.out.printf("original arrays of numbers:"+Arrays.tostring(nums));
   boolean result = test(num);
   Sytem.out.print(" "+result);
   {
   public static void main(int[] nums){
   long count_negitive,count_positive;
   nums=Arrays.stream(nums).distinct().toArray();
   count_negitive =arrays.stream(nums).filter(s->s<0).count();
   count_positive =arrays.stream(nums).filter(s->s>0).count();
   return count_negitive>count_positive;
   }
   }
  }
}